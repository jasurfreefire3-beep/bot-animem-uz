# Northflank Deployment Configuration
# Supports both Dockerfile build and buildpack/Node.js direct build
processes:
  web:
    command: npm start
    port: 3000
    healthCheck:
      path: /api/health
      port: 3000
